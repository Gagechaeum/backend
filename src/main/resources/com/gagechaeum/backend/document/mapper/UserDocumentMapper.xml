<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gagechaeum.backend.document.mapper.UserDocumentMapper">

  <resultMap id="userDocumentResultMap" type="com.gagechaeum.backend.document.domain.UserDocument">
    <id property="userDocumentId" column="user_document_id"/>
    <result property="userId" column="user_id"/>
    <result property="documentId" column="document_id"/>
    <result property="documentName" column="document_name"/>
    <result property="issuedAt" column="issued_at"/>
    <result property="fileKey" column="file_key"/>
  </resultMap>

  <select id="findUserDocumentsByUserId" parameterType="long" resultMap="userDocumentResultMap">
    SELECT
      user_document_id,
      user_id,
      document_id,
      document_name,
      issued_at,
      file_key
    FROM
      user_documents
    WHERE
      user_id = #{userId}
  </select>

  <insert id="insert" parameterType="UserDocument"
    useGeneratedKeys="true" keyProperty="userDocumentId">
    INSERT
    INTO user_documents (user_id, document_id, document_name, issued_at, file_key)
    VALUES (#{userId}, #{documentId}, #{documentName}, #{issuedAt}, #{fileKey})
  </insert>

  <select id="getById" resultType="UserDocument">
    SELECT user_document_id, document_id, document_name, issued_at, file_key
    FROM user_documents
    WHERE user_id = #{userId} AND user_document_id = #{userDocumentId}
  </select>

  <select id="getFileKeyById" resultType="java.lang.String">
    SELECT file_key
    FROM user_documents
    WHERE user_id = #{userId} AND user_document_id = #{userDocumentId}
  </select>

  <delete id="deleteById">
    DELETE
    FROM user_documents
    WHERE user_id = #{userId} AND user_document_id = #{userDocumentId}
  </delete>

  <delete id="deleteByName">
    DELETE
    FROM user_documents
    WHERE user_id = #{userId} AND document_name = #{documentName}
  </delete>

</mapper>
