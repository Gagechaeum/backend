<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gagechaeum.backend.document.mapper.UserDocumentMapper">

  <insert id="insert" parameterType="UserDocument"
    useGeneratedKeys="true" keyProperty="userDocumentId">
    INSERT
    INTO user_documents (user_id, document_id, document_name, issued_at, file_url)
    VALUES (#{userId}, #{documentId}, #{documentName}, #{issuedAt}, #{fileUrl})
  </insert>

  <select id="getFileUrlById" parameterType="UserDocument" resultType="java.lang.String">
    SELECT file_url
    FROM user_documents
    WHERE user_id = #{userId} AND user_document_id = #{userDocumentId}
  </select>

  <delete id="deleteById" parameterType="UserDocument">
    DELETE
    FROM user_documents
    WHERE user_id = #{userId} AND user_document_id = #{userDocumentId}
  </delete>
</mapper>
