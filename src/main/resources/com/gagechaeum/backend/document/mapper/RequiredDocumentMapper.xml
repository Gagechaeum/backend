<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gagechaeum.backend.document.mapper.RequiredDocumentMapper">

    <insert id="save" parameterType="com.gagechaeum.backend.document.domain.RequiredDocument">
        INSERT INTO required_documents (document_id, policy_id)
        SELECT #{documentId}, #{policyId}
            WHERE NOT EXISTS (
        SELECT 1
        FROM required_documents
        WHERE document_id = #{documentId} AND policy_id = #{policyId}
                )
    </insert>

</mapper>
