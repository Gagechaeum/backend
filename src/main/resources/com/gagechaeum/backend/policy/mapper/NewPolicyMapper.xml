<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gagechaeum.backend.policy.mapper.NewPolicyMapper">

    <insert id="savePolicyDetail" parameterType="com.gagechaeum.backend.policy.domain.NewPolicyDetail">
        INSERT INTO policy_details_temp (policy_id, raw_text, updated_at)
        VALUES (#{policyId}, #{rawText}, #{updatedAt})
            ON DUPLICATE KEY UPDATE
                                 raw_text = VALUES(raw_text),
                                 updated_at = VALUES(updated_at)
    </insert>

</mapper>
